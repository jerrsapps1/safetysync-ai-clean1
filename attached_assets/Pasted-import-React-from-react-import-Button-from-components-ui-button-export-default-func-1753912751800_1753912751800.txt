import React from "react";
import { Button } from "../components/ui/button";

export default function InstructorsPage() {
  return (
    <div className="bg-white text-gray-900 font-sans">
      {/* Header */}
      <section className="bg-gradient-to-r from-blue-700 to-blue-500 text-white text-center py-20 px-6">
        <h1 className="text-4xl md:text-5xl font-bold mb-4">Designed for Instructors & Safety Consultants</h1>
        <p className="text-lg max-w-2xl mx-auto">
          Manage your training clients, generate OSHA-compliant certificates, and streamline every session from sign-in to audit—all in one platform.
        </p>
      </section>

      {/* Problem vs. Solution */}
      <section className="py-16 px-6 max-w-6xl mx-auto grid md:grid-cols-2 gap-10">
        <div>
          <h2 className="text-2xl font-bold mb-4">The Problem</h2>
          <ul className="list-disc list-inside text-gray-700 space-y-2">
            <li>Managing multiple clients manually</li>
            <li>Certificates are time-consuming and error-prone</li>
            <li>No centralized history for training records</li>
            <li>Audits and follow-ups are difficult to manage</li>
          </ul>
        </div>
        <div>
          <h2 className="text-2xl font-bold mb-4">How SafetySync Helps</h2>
          <ul className="list-disc list-inside text-blue-700 space-y-2">
            <li>Manage multiple companies and locations in one dashboard</li>
            <li>Generate and email certificates instantly with QR code verification</li>
            <li>Instructor portal with CRUD access to all training records</li>
            <li>Auto-notify clients when trainings are due for renewal</li>
          </ul>
        </div>
      </section>

      {/* Features */}
      <section className="bg-gray-50 py-16 px-6">
        <h2 className="text-3xl font-bold text-center mb-12">Powerful Features for Instructors</h2>
        <div className="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
          {[
            {
              title: "Instructor Portal",
              desc: "Create, edit, and track all training sessions across clients. Upload files and update employee records with ease.",
            },
            {
              title: "Certificate & Wallet Card Generator",
              desc: "Instantly issue branded certificates with QR-code verification. Print or send digitally.",
            },
            {
              title: "Sign-In Sheet Automation",
              desc: "Auto-generate OSHA-compliant sign-in sheets by course and date, pre-filled with attendees.",
            },
          ].map((feature, idx) => (
            <div key={idx} className="bg-white p-6 rounded-xl shadow border hover:shadow-md transition">
              <h3 className="text-xl font-semibold mb-2">{feature.title}</h3>
              <p>{feature.desc}</p>
            </div>
          ))}
        </div>
      </section>

      {/* CTA */}
      <section className="bg-blue-900 text-white text-center py-20 px-6">
        <h2 className="text-3xl font-bold mb-4">Run Your Training Business Like a Pro</h2>
        <p className="text-lg max-w-xl mx-auto mb-8">
          Whether you’re a solo consultant or training center, SafetySync gives you the tools to run smoother sessions and deliver professional, audit-ready results.
        </p>
        <Button className="bg-white text-blue-800 hover:bg-gray-100 text-lg font-semibold px-6 py-3 rounded-xl">
          Try It Free as an Instructor
        </Button>
      </section>
    </div>
  );
}
